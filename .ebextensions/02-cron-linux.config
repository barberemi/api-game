files:
  "/etc/cron.d/mycron":
    mode: "000644"
    owner: root
    group: root
    content: |
      0 0 * * * root cd /var/www/html/ && bin/console app:user-reset-actions

commands:
  remove_old_cron:
    command: rm -f /etc/cron.d/mycron.bak